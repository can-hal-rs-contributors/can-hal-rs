[package]
name = "can-hal-kvaser-examples"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
can-hal = { path = "../../can-hal", package = "can-hal-rs" }
can-hal-kvaser = { path = "../../can-hal-kvaser" }
