[package]
name = "can-hal-socketcan-examples"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
can-hal = { path = "../../can-hal", package = "can-hal-rs" }
can-hal-socketcan = { path = "../../can-hal-socketcan" }
