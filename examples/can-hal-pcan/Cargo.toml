[package]
name = "can-hal-pcan-examples"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
can-hal = { path = "../../can-hal", package = "can-hal-rs" }
can-hal-pcan = { path = "../../can-hal-pcan" }
